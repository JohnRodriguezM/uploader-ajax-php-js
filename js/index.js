/* const getData = async () => {
  const response = await fetch('https://api.github.com/users/miguelmota',
    {
        method: 'POST',
        headers: {},
        body: JSON.stringify({}),
    });
  const data = await response.json();
  console.log(data);
};

document.addEventListener('DOMContentLoaded', getData);

 */

